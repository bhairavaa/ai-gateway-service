# --- Web framework ---
fastapi==0.139.2
uvicorn[standard]==0.51.0

# --- Configuration & validation ---
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Database layer (async SQLAlchemy, SQLite today, Postgres-ready) ---
sqlalchemy[asyncio]==2.0.51
aiosqlite==0.22.1
alembic==1.18.5

# --- Rate limiting store ---
redis==8.0.1

# --- LLM providers: stable, dedicated LangChain integration packages ---
# (deliberately NOT langchain-community — those wrap unstable/community-maintained integrations)
# Pinned to the current 1.x LangChain line (langchain-core moved from 0.3.x to a 1.0 major
# release) — verified as a mutually-compatible set via a real dependency resolution, not just
# picked from memory.
langchain-core==1.4.9
langchain-openai==1.3.5
langchain-anthropic==1.4.8
langchain-google-genai==4.2.7
langchain-ollama==1.1.0

# Used directly (not just via langchain-openai/langchain-anthropic internals) for provider health checks.
# Version floors match what langchain-openai 1.3.5 / langchain-anthropic 1.4.8 themselves require.
openai>=2.45.0,<3.0.0
anthropic>=0.96.0,<1.0.0

# --- HTTP client (used directly for provider health checks) ---
httpx==0.28.1
